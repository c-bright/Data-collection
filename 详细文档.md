# 智能电商数据分析平台项目说明

## 1. 项目概述

`E:\test` 是一个前后端分离的电商数据分析项目，围绕京东商品构建了一条相对完整的业务链路：

1. 用户登录与注册
2. 根据关键词抓取商品列表
3. 查看商品详情与规格参数
4. 抓取差评与标签数据
5. 生成前端可视化分析结果
6. 在前端展示商品分析与评论分析图表

从代码实现上看，这个项目更接近“课程设计 / 毕业设计 / 本地演示型系统”，而不是直接可上线的生产级平台。它已经具备完整的页面、接口、数据库模型、爬虫链路和分析链路，但同时也保留了不少本地环境依赖与硬编码配置。

## 2. 项目整体结构

`E:\test` 根目录下包含两个核心子项目：

```text
E:\test
├─ smart_data_platform_backend/    # Flask + SQLAlchemy + Scrapy/Playwright 后端
└─ smart_data_platform_frontend/   # Vue 3 + Vite 前端
```

推荐将它理解为一个双仓结构：

- `smart_data_platform_backend` 负责接口、数据库访问、爬虫执行、评论清洗、分析结果生成
- `smart_data_platform_frontend` 负责登录页、任务页、详情页、图表页及接口调用

## 3. 技术栈

### 前端

- Vue 3
- Vite 7
- Vue Router 4
- Axios
- ECharts
- echarts-wordcloud
- Docker / Nginx

### 后端

- Flask
- Flask-CORS
- SQLAlchemy
- PyMySQL
- Scrapy
- Selenium
- Playwright
- playwright-stealth
- Pandas
- Pydantic

### 数据与运行环境

- MySQL
- Edge WebDriver / Chromium
- Playwright 浏览器环境

## 4. 业务流程

### 4.1 登录注册流程

前端 `Login.vue` 提供登录和注册两种模式：

- 注册调用 `POST /api/register`
- 登录调用 `POST /api/login`
- 前端通过 `localStorage.setItem('token', 'true')` 记录登录状态
- 路由守卫基于本地 `token` 控制访问权限

这意味着当前认证方式是“前端本地状态 + 后端明文密码校验”的轻量实现，适合演示，不适合生产。

### 4.2 商品抓取流程

用户在前端抓取页输入关键词和页数后：

1. 前端调用 `POST /api/crawl/start`
2. 后端检查数据库中是否已有该关键词分类数据
3. 若没有，则启动独立进程执行 Scrapy 爬虫
4. 前端轮询 `GET /api/crawl/status?keyword=...`
5. 爬虫结束后，结果写入 JSON 与 MySQL
6. 后端返回商品列表，前端分页展示商品卡片

### 4.3 商品详情与分析流程

用户点击某个商品后：

1. 前端跳转到 `/detail/:id`
2. 页面调用 `GET /api/analysis/full/<product_id>`
3. 后端先从数据库读取商品基础信息
4. 使用 Playwright 打开商品详情页抓取规格参数
5. 再抓取差评与标签
6. 清洗评论与标签数据
7. 生成商品散点图、价格分布、店铺分布、差评词云、标签柱状图所需 JSON
8. 返回统一结果给前端
9. 前端同步刷新详情页、商品分析页、评论分析页

这个接口是整个系统最核心的聚合接口：`GET /api/analysis/full/<product_id>`。

## 5. 前端结构说明

### 5.1 目录结构

```text
smart_data_platform_frontend/
├─ docker/
│  ├─ backend.Dockerfile
│  └─ nginx.conf
├─ public/
├─ src/
│  ├─ assets/
│  ├─ components/
│  │  ├─ ChartCard.vue
│  │  ├─ ParamTable.vue
│  │  ├─ ProductCard.vue
│  │  ├─ Sidebar.vue
│  │  ├─ SourceBadge.vue
│  │  ├─ StatusPanel.vue
│  │  └─ TopBar.vue
│  ├─ layout/
│  │  └─ MainLayout.vue
│  ├─ router/
│  │  └─ index.js
│  ├─ store/
│  │  └─ index.js
│  ├─ utils/
│  │  └─ request.js
│  ├─ views/
│  │  ├─ AnalysisComment.vue
│  │  ├─ AnalysisProduct.vue
│  │  ├─ Crawl.vue
│  │  ├─ Detail.vue
│  │  ├─ Home.vue
│  │  └─ Login.vue
│  ├─ App.vue
│  ├─ main.js
│  └─ style.css
├─ docker-compose.yml
├─ package.json
├─ package-lock.json
├─ README.md
└─ vite.config.js
```

### 5.2 页面职责

- `Login.vue`
  - 登录 / 注册页面
  - 包含前端滑块验证
  - 登录成功后将状态写入 `localStorage`

- `Home.vue`
  - 首页介绍
  - 通过卡片方式展示平台模块能力

- `Crawl.vue`
  - 商品抓取任务创建页
  - 调用后端抓取接口
  - 轮询任务状态
  - 展示商品列表和分页结果

- `Detail.vue`
  - 商品详情页
  - 展示基础信息、价格、店铺、评论量、规格参数
  - 页面加载时触发完整分析接口

- `AnalysisProduct.vue`
  - 商品分析页
  - 展示价格与评论数散点图
  - 展示价格分布柱状图
  - 展示店铺分布饼图

- `AnalysisComment.vue`
  - 评论分析页
  - 展示差评词云
  - 展示标签统计柱状图

### 5.3 前端路由

主要路由如下：

- `/login`
- `/`
- `/crawl`
- `/detail/:id`
- `/analysis/product`
- `/analysis/comment`

其中：

- `/login` 是公开路由
- 其余页面都依赖 `requiresAuth`
- 路由守卫通过 `localStorage.getItem('token') === 'true'` 判断是否登录

### 5.4 前端状态管理

项目没有使用 Pinia/Vuex，而是自行在 `src/store/index.js` 中实现了一个轻量全局状态容器，主要保存：

- 商品列表
- 抓取任务状态
- 当前商品详情
- 分析状态
- 数据来源状态（sample / loading / real）
- 商品分析数据
- 评论分析数据

它还内置了两类能力：

- 调用真实后端接口
- 当接口失败时退回示例数据

这让前端在后端不稳定时仍可演示图表页面。

### 5.5 前端接口代理

`vite.config.js` 中将 `/api` 代理到：

```text
http://127.0.0.1:5000
```

因此本地开发时前后端默认端口为：

- 前端：`5173`（Vite 默认）
- 后端：`5000`

## 6. 后端结构说明

### 6.1 目录结构

```text
smart_data_platform_backend/
├─ app/
│  ├─ analysis/
│  │  ├─ comment_analyzer.py
│  │  ├─ product_stats_analyzer.py
│  │  ├─ TagAnalyzer.py
│  │  └─ test_1.py
│  ├─ api/
│  │  ├─ analysis_api.py
│  │  ├─ crawler_api.py
│  │  └─ login.py
│  ├─ crawler/
│  │  ├─ chromedriver.exe
│  │  ├─ msedgedriver.exe
│  │  └─ scrapy_spiders/
│  │     └─ jd_scrapy_demo/
│  ├─ models/
│  │  ├─ product_model.py
│  │  └─ user_model.py
│  ├─ schemas/
│  │  ├─ analysis_schema.py
│  │  ├─ product_schema.py
│  │  └─ response.py
│  ├─ services/
│  │  ├─ analysis_service.py
│  │  └─ data_service.py
│  ├─ utils/
│  │  ├─ auth.json
│  │  ├─ da.json
│  │  ├─ jd.json
│  │  ├─ jd_scatter_comment.json
│  │  ├─ redis_config.py
│  │  └─ database/
│  │     ├─ config.ini
│  │     ├─ db_manager.py
│  │     └─ sql/
│  └─ __init__.py
├─ docker/
│  ├─ Dockerfile
│  └─ docker-compose.yml
├─ scripts/
├─ tests/
├─ jd_data.json
├─ main.py
├─ README.md
├─ requirements.txt
└─ venv/
```

### 6.2 核心模块职责

#### `main.py`

后端入口文件，负责：

- 创建 Flask 应用
- 启用跨域支持
- 注册蓝图

当前注册的蓝图包括：

- `login_bp`
- `crawler_bp`
- `analysis_bp`

### 6.3 API 层

#### 认证接口

文件：`app/api/login.py`

- `POST /api/register`
  - 用户注册
  - 检查用户名是否重复
  - 邮箱唯一性依赖数据库约束

- `POST /api/login`
  - 用户登录
  - 按用户名查询
  - 明文密码比对

#### 爬虫接口

文件：`app/api/crawler_api.py`

- `POST /api/crawl/start`
  - 启动抓取任务
  - 若数据库已有该关键词数据则直接返回缓存结果
  - 若任务已在执行则返回 `crawling`
  - 否则以 `multiprocessing.Process` 启动爬虫

- `GET /api/crawl/status`
  - 查询抓取状态
  - 若进程还活着则返回 `crawling`
  - 若数据库已有结果则返回 `completed`

#### 分析接口

文件：`app/api/analysis_api.py`

- `POST /api/analysis/run`
  - 单独触发分析任务

- `GET /api/analysis/detail/<product_id>`
  - 返回基础详情 + 动态抓取到的规格参数

- `GET /api/analysis/full/<product_id>`
  - 返回完整详情与分析结果
  - 这是前端详情页最关键接口

### 6.4 模型层

#### `Product`

文件：`app/models/product_model.py`

主要字段：

- `product_id`
- `name`
- `price`
- `link`
- `image`
- `shop`
- `category`
- `comment_count`

#### `User`

文件：`app/models/user_model.py`

主要字段：

- `id`
- `username`
- `password`
- `email`

### 6.5 数据库管理

文件：`app/utils/database/db_manager.py`

职责包括：

- 读取 `config.ini`
- 创建 SQLAlchemy `engine`
- 创建线程安全 `scoped_session`
- 创建数据表
- 插入商品
- 查询商品
- 更新商品属性

数据库配置文件位于：

```text
app/utils/database/config.ini
```

### 6.6 爬虫链路

#### 列表抓取

入口文件：

```text
app/crawler/scrapy_spiders/jd_scrapy_demo/run_spider_script.py
```

核心 spider：

```text
app/crawler/scrapy_spiders/jd_scrapy_demo/jd_scrapy_demo/spiders/jd.py
```

功能：

- 按关键词访问京东搜索页
- 解析商品卡片
- 提取商品 ID、标题、价格、店铺、评论数、图片、链接
- 通过中间件和 Selenium 辅助翻页或登录处理

#### 商品详情与评论抓取

核心文件：

```text
app/crawler/scrapy_spiders/jd_scrapy_demo/jd_scrapy_demo/selenium_spiders/datil.py
```

主要功能：

- 用 Playwright 打开商品详情页
- 必要时等待人工登录
- 从 `auth.json` 复用登录状态
- 抓取商品规格参数
- 进入评论区
- 提取标签与差评内容
- 生成 `datil.json`

### 6.7 数据清洗与分析链路

#### `DataService`

文件：`app/services/data_service.py`

负责：

- 清洗商品字段
- 生成 JSONL
- 转换为标准 JSON
- 导入 MySQL

#### `JDReviewProcessor`

同样位于 `app/services/data_service.py`，负责：

- 清洗标签
- 清洗评论文本
- 删除原始评论文件
- 输出清洗后评论文件

#### `ScatterCommentService`

文件：`app/services/analysis_service.py`

负责将：

- 商品散点数据
- 评论文本
- 标签数据

组合成统一的中间文件 `jd_scatter_comment.json`。

#### `FrontendDataService`

文件：`app/analysis/test_1.py`

负责把中间数据进一步转换为前端图表所需结构，输出：

```text
app/utils/da.json
```

它内部依赖：

- `ProductStatsAnalyzer`
- `CommentAnalyzer`
- `TagAnalyzer`

最终生成三类分析结果：

- 商品分析
- 评论词云
- 标签统计

## 7. 重要中间文件说明

项目运行过程中会在 `app/utils/` 中生成或使用多个中间文件：

- `auth.json`
  - Playwright 登录状态缓存

- `jd.json`
  - 商品列表标准 JSON

- `datil.json`
  - 原始评论与标签抓取结果

- `datil_list.json`
  - 清洗后的评论与标签数据

- `jd_scatter_comment.json`
  - 商品散点 + 评论 + 标签融合结果

- `da.json`
  - 前端最终可视化数据

这些文件说明系统并不是“数据库全托管”架构，而是“数据库 + 本地 JSON 中间产物”的混合模式。

## 8. 接口总览

### 8.1 认证接口

```http
POST /api/register
POST /api/login
```

### 8.2 爬虫接口

```http
POST /api/crawl/start
GET /api/crawl/status?keyword=xxx
```

示例请求：

```json
{
  "keyword": "耳机",
  "max_page": 2
}
```

### 8.3 分析接口

```http
POST /api/analysis/run
GET /api/analysis/detail/<product_id>
GET /api/analysis/full/<product_id>
```

## 9. 部署与运行方式

## 9.1 后端本地运行

进入后端目录后：

```bash
pip install -r requirements.txt
python main.py
```

默认地址：

```text
http://127.0.0.1:5000
```

### 9.2 前端本地运行

进入前端目录后：

```bash
npm install
npm run dev
```

### 9.3 数据库准备

需要先配置：

```text
app/utils/database/config.ini
```

至少要确认以下字段：

- `host`
- `port`
- `user`
- `password`
- `database`
- `charset`

### 9.4 Playwright 浏览器准备

安装 Python 依赖后，还需要安装浏览器内核：

```bash
playwright install chromium
```

### 9.5 WebDriver 依赖

项目当前明显依赖本地浏览器驱动：

- `app/crawler/msedgedriver.exe`
- `app/crawler/chromedriver.exe`

这意味着它对本机环境有较强耦合。

## 10. Docker 相关情况

项目内存在两套 Docker 配置，但成熟度不一致。

### 10.1 前端目录中的 Docker 编排

`smart_data_platform_frontend/docker-compose.yml` 较完整，包含：

- `frontend`
- `backend`
- `mysql`

并通过 `nginx.conf` 将 `/api/` 反向代理到 `backend:5000/api/`。

### 10.2 后端目录中的 Docker 配置

`smart_data_platform_backend/docker/Dockerfile` 和 `docker-compose.yml` 看起来像另一套早期尝试，存在明显不一致：

- 使用 `uvicorn main:app`
- 但实际后端入口是 Flask `create_app()` / `app.run(...)`
- `docker-compose.yml` 暴露 `8000`
- 与实际后端默认 `5000` 不一致

因此如果要部署，优先参考前端目录下那套 Compose 配置，而不是后端目录中的旧配置。

## 11. 项目优点

这个项目的完成度其实不低，优点主要有：

- 前后端链路完整，能跑通“抓取 - 存储 - 分析 - 展示”
- 页面结构清晰，模块划分明确
- 后端已按 `api / models / services / analysis / crawler` 分层
- 前端通过统一 store 管理异步状态
- 详情接口做了聚合，前端调用简单
- 支持评论标签、词云、价格分布等可视化能力
- 对登录态做了本地缓存，能减少重复扫码/登录

## 12. 当前问题与风险

从代码实现上看，下面这些点需要特别注意：

### 12.1 存在大量硬编码绝对路径

例如多个后端文件直接写死：

```text
E:\test\smart_data_platform_backend\app\utils
```

这会导致：

- 项目几乎无法直接跨机器运行
- Docker 环境兼容性差
- 上传 GitHub 后别人克隆下来不能直接用

### 12.2 明文密码存储与校验

登录和注册逻辑当前是：

- 注册时直接存明文密码
- 登录时直接明文比对

这在生产环境是严重安全问题。

### 12.3 登录态依赖本地文件

`auth.json` 保存了浏览器登录态，这对本地演示方便，但：

- 不适合提交到公开仓库
- 也不适合多人协作环境

### 12.4 爬虫链路依赖人工参与

代码里明确处理了“跳到登录页后等待人工登录”的流程，因此这不是完全无人值守抓取系统。

### 12.5 中间文件过多

分析链路依赖多个 JSON 文件流转，虽然实现直观，但会带来：

- 文件状态不一致
- 重复运行覆盖问题
- 并发执行风险

### 12.6 Docker 配置不统一

后端目录下和前端目录下的容器配置思路不一致，说明容器化尚未完全收敛。

## 13. 上传 GitHub 前建议清理

如果要将这个项目上传 GitHub，建议至少处理以下内容：

### 必须忽略的目录与文件

- `smart_data_platform_frontend/node_modules/`
- `smart_data_platform_backend/venv/`
- `__pycache__/`
- `*.pyc`
- `auth.json`
- `app/utils/*.json` 中的运行产物
- 数据库连接配置文件中的敏感信息

### 建议补充 `.gitignore`

至少应覆盖：

```gitignore
node_modules/
venv/
__pycache__/
*.pyc
.DS_Store
app/utils/auth.json
app/utils/jd.json
app/utils/datil.json
app/utils/datil_list.json
app/utils/jd_scatter_comment.json
app/utils/da.json
app/utils/database/config.ini
```

## 14. 推荐改进方向

如果后续继续维护，建议按下面顺序优化：

1. 把所有绝对路径改成相对路径或环境变量
2. 用 `.env` 管理数据库和路径配置
3. 为密码引入哈希存储，例如 `werkzeug.security`
4. 将登录态缓存、分析输出目录改成可配置
5. 统一 Docker 配置，只保留一套可运行版本
6. 把 JSON 中间文件流改造成数据库或任务缓存流
7. 为抓取与分析链路补测试
8. 给接口补充统一错误码和日志系统

## 15. 适合 GitHub 的项目定位说明

如果你准备把这个项目上传 GitHub，建议把它定位成：

> 一个基于 Vue 3 + Flask + Scrapy/Playwright 的京东商品抓取与评论分析演示平台，支持商品列表采集、商品详情抓取、评论清洗、标签分析及多维可视化展示。

这个定位比较准确，也符合当前代码成熟度。

## 16. 总结

这是一个“前后端分离 + 爬虫抓取 + 数据分析 + 图表展示”一体化项目。它最大的价值不只是做了几个页面，而是把抓取、数据库、分析和可视化串成了完整闭环。对于课程项目、毕业设计、作品集展示来说，完成度是不错的。

如果以 GitHub 展示为目标，这份代码已经具备很好的基础，但在提交前一定要做好以下三件事：

1. 清理本地依赖和敏感文件
2. 去掉硬编码路径
3. 在 README 中明确“这是本地演示型项目”的定位和运行前提

---

如果你愿意，我下一步可以继续帮你做两件事中的任意一件：

1. 直接把这份文档放到 `E:\test\README.md`
2. 顺手再帮你生成一份适合上传 GitHub 的 `.gitignore`
